
//// initialis truffle with its containing folders
truffle init 


//// create truffle migrations

truffle create migration contract name



//// configure migration file for deployment

configure / modify your migration file

//// Compile your contract into byecode for evn and produce abi your migration for 

truffle compile

/// After Compiling  testnet should be initialised contract make use of the truffle develop command
truffle develop

/// Run Migrate 


Migrate 


//// Deploy contract ///

FirstContract.deployed().then((instance)=> {inst = instance:});

/// type variable to point to smart contract
inst:

/// call the smart contract and check for string 
inst.getMessage().then(console.log);"
"